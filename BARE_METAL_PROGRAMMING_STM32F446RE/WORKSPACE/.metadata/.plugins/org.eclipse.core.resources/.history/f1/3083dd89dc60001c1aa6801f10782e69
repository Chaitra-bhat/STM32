
#include<adc.h>
#include<uart.h>
#include<stdint.h>

uint32_t sensor_value;

int main(void)
{
	adc_init();
	usart2_txRx_init();
	start_conversion();

	while(1)
	{
		sensor_value = adc_read();
		printf("ADC conversion value: %d\r\n", sensor_value);
	}

}
