#include<uart.h>

char data;

int main()
{
	//For LED blinking
	RCC->AHB1ENR |= GPIOD_ENA;

	//Enable port12 as output functionality to blink LED
	GPIOD->MODER |= (1U<<24);
	GPIOD->MODER &= ~(1U<<25);


	uart_RXTX_init();

	while(1)
	{
		data = uart_read();
	}
	return 0;
}

